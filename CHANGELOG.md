# Changelog

Add a new timestamped entry after implementing changes. Record user-visible behavior, meaningful verification, and material limitations. Research and design discussion belong in the indexed [reports](reports/README.md); private manuscript and transcript content never belong in this log.

## 2026-09-14 17:56 UTC

- Renamed the local tool to **Annotate + Demarcate • Local Development**, revised titles and styling, and added pseudonymized source previews plus scoped macOS Finder/original-document actions.
- Added an indexed report on historical consumer Copilot memory, other conversational interfaces, and a proposed memory-representation intervention. Original Copilot memory internals remain unknown; automatic summarization is a documented proposal, not an implemented control.
- Added About and Read the Essay navigation, preserving the current exploration and unfinished intervention. The essay's backend toggle hides its tab and disables direct content access. The reading is enabled locally; public essay access remains off pending confirmation.
- Added a private TeX/BibTeX-to-HTML workflow with citations, bibliography, editable name macros, standalone aliased HTML export, pinned private storage, and shared pseudonymization for both readings. Manuscript files, About text, and generated releases stay outside Git and the frontend build.
- Added backend and browser regressions for source actions, editorial visibility, aliases, HTML safety, citations, navigation, mobile accessibility, and content boundaries. CI now exercises Pandoc conversion using invented material. Local verification: 48 backend tests, eight browser regressions, and a production frontend build.
- Added report-index and timestamped changelog guidance to development documentation and the local agent notes.

## 2026-09-21 07:15 UTC

- Added a backend counterfactual experiment runner that simulates both the Traveler and companion at the three curated beginnings. The default paired design has two original seed interventions per event and 12 conditions, covering full history, generated compaction, rolling branch memory, three characterization scopes, four future-insertion policies, and independent OpenAI, Anthropic, and Gemini actor assignments.
- Reused the existing provider adapters, private aliases, and beginning catalog. Added exact native payload receipts, character and summary dependencies, dynamic lexical retrieval, hypothetical future adaptation, explicit budget failures, retained partial/retry responses, configuration fingerprints, resumable cases, private comparison reports, and a receipt auditor. Simulated output remains separate from historical content and the interactive companion-only policy.
- Live testing exposed overlong compaction outputs, source-header imitation, and transient transport failures. Revised compaction instructions and the builder allowance, kept provenance outside actor dialogue examples, and added one bounded transport retry with preserved attempts. The final 432 simulated turns contained no detected source-ID echoes.
- Verification: 75 backend tests passed, including 27 experiment tests; the frontend production build passed; all 72 offline cases completed and were reused on resume; all 72 live cases completed after restarting two transport-failed cases. The live audit verified 612 saved receipt entries including shared preparation copies and retry attempts. Private original failures remain available locally.
- At May 8 and May 10, compact memory reduced the measured mean dialogue input bound by 91.1% and 94.1%, respectively. All six rolling-memory branches compacted earlier simulated turns. These are structural and input-size measurements, not evidence of semantic equivalence, character fidelity, or provider superiority. Retrieval uses bounded leading excerpts and character sketches use sampled human evidence; both remain experimental.
- Added the indexed research report, execution guide, shareable suite, `just counterfactual` command, ignored owner-only result files, and public-tree detection for private experiment exports.

## 2026-09-21 17:26 UTC

- Added a minimal, independent results dashboard at `http://127.0.0.1:8002`, launched with `just results`. The read-only local server and separate React/Vite build do not mount routes in or alter the interactive experience.
- The dashboard defaults to the final 72-case screening and offers run selection, event/seed/condition/search filters, matched memory comparisons, complete simulated dialogue, comparison with the matching baseline, characterization and memory inspection, per-turn future material, on-demand exact receipts, and full case downloads. Earlier failed cases and offline fixtures remain distinct.
- Private result files stay outside the frontend build. Added loopback, host, origin, cross-site, plan-membership, file-identity, and path/symlink checks; disabled hosted use and browser caching; rendered saved Markdown without raw HTML or remote images.
- Verification: 80 backend tests passed, including five new dashboard tests; the standalone production build passed. In-app browser checks covered filtering, baseline comparison, context/adaptation, exact receipt loading, offline and failed runs, empty-search recovery, and 1536×1024 / 390×844 layouts. No console warnings or errors were observed, and mobile content did not overflow horizontally. The final screening remains open in the in-app browser.
- Charts report conservative byte bounds for completed baseline/compact pairs matched by event, seed, and repetition. They follow event and seed filters; they do not claim semantic equivalence or character fidelity. The server is local-only and must be running to inspect results.

## 2026-09-21 21:00 UTC

- Added scripted two-anchor path tracing across April 1 → May 8, May 8 → May 10, and April 1 → May 10. The 18-case design compares upfront, backward-planned, midpoint, and companion-only endpoint disclosure, plus Anthropic companion and Gemini Traveler substitutions. Each case generates eight turns between fixed recorded anchors while withholding the intervening and later archive.
- Added strict path-suite validation, endpoint provenance, per-turn disclosure receipts, backward waypoint plans, and separately labeled model handoff critiques. Retrospective persona evidence and ordinary future retrieval are rejected in this holdout design. Review failures remain independent of dialogue completion.
- Completed an 18-case live pilot and an 18-case revised screening, totaling 288 simulated turns. The pilot exposed premature endpoint enactment and changing interlocutors. Revised reference placement and chronology instructions improved inspected openings but did not eliminate chronology, role, or endpoint-handoff errors; model reviews sometimes missed those errors. Both versions remain locally inspectable, without claims of semantic validation or provider superiority.
- Updated the independent results dashboard with interval summaries, separately labeled source anchors, clickable endpoint-visibility cells, waypoint and critique inspection, and matched comparison with upfront disclosure. The revised path-tracing run opens by default when present; the original 72-case counterfactual run remains accessible.
- Verification: 88 backend tests and the dashboard production build passed. Both live batches passed structural/native-payload audits of 18 cases and 225 receipt references each. The revised screening completed 174 unique model calls with no retries and all 18 reviews. All 18 refined offline fixtures completed. In-app browser checks verified filters, turn-five exposure, paired dialogue comparison, exact endpoint requests, original-run compatibility, and 1536×1024 / 390×844 layouts, with no observed browser warnings/errors or mobile horizontal overflow.
- Added the public path-tracing suite, execution documentation, and indexed research report. Private source material, generated dialogue, and receipts remain in ignored local results storage. Completion denotes generated turn count, not a validated path to the endpoint.

## 2026-09-22 03:10 UTC

- Added an event-only extraction and conservative semantic-review pipeline over all 369 dialogue messages and two annotation sources. The final private asset contains 221 primary events (183 reported, 38 planned), plus two earliest-boundary records. Literal evidence, source hashes, full preprocessing scopes, original candidates, review rejections, and exact native receipts are retained. As-of simulation context excludes any event whose preprocessing saw unavailable sources.
- Added the main experience’s animated Events view with playback, scrubbing, status filters, model-review labels, evidence, source-conversation navigation, reduced-motion support, and stable ordering by disclosure and source sequence. Occurrence dates remain unknown for 196 primary events. Extraction and model review do not establish exhaustive recall or human verification; semantic duplicates and a known time-until-nightfall/duration ambiguity remain documented.
- Added a 36-case script-path design comparing one scriptwriter with two character agents, fixed eight-turn controls with model-chosen stopping, dialogue/annotation/event-only inputs, state-first planning, compaction, and an Anthropic companion. Structured outputs preserve hypothetical physical events and separate character-knowledge fields. Native output, validation errors, and one bounded repair attempt are retained; operational ceilings and hindsight exposure remain explicit.
- Executed all 36 live cases: 34 completed, two retained structured-output failures, 354 generated turns, and 137 hypothetical playback events. Completed adaptive scripts chose 5–19 turns; none reached the 24-turn ceiling. All 34 completed scripts received a model critique. Selected qualitative inspection still found premature endpoint enactment and overpermissive critiques; completion is not evidence of a valid chronological bridge.
- Added a Setup graph to every dashboard result, with actual agent/model rosters, exact prompt/context previews, extraction/review/summary/plan dependencies, native receipt export, and editable suite cloning. Added simulated event playback and comparison with the same architecture’s adaptive-dialogue control. The new screening is the default run; older runs and failures remain inspectable.
- Verification: 117 backend tests, both production builds, six focused browser tests, all 36 offline fixtures, and structural audits of all 36 live cases and 345 receipt references passed. The source timeline audit reconstructed 166 extraction/review receipts. Desktop/mobile, reduced-motion, accessibility, exact-context inspection, exports, lineage navigation, and playback checks passed. Two dependency deprecation warnings remain. Added report 004 and execution documentation; private material remains outside the public tree.

## 2026-09-22 05:42 UTC

- Applied the shared private alias configuration at the event-timeline and results-dashboard response boundaries, including generated text, evidence, prompts, context, graph metadata, receipts, and downloads. Removed original participant-name literals from experimental instructions. The dashboard now loads the same server environment as the main application.
- Kept original saved artifacts immutable. Changed displays explicitly identify alias projection; original source/manifest/payload hashes remain attached to the saved records, and changed native payloads carry a separate display hash. Browser exports use the same aliased projection rather than serving raw private files.
- Made setup graph nodes open immediate, searchable preview dialogs with readable prompt/context blocks, response text, provenance, preprocessing links, native payloads, keyboard focus restoration, and mobile sizing.
- Added a formatted per-result Viewer and focused Open Result reading mode, run/case deep links, turn navigation, distinct recorded anchors, per-turn agent/model/context previews on hover or focus, click-to-pin support, prompt inspection, and direct request links into Setup. Agent previews use a viewport-bounded top layer to avoid clipping inside the scenario inspector.
- Verification: 120 backend tests passed, the 36-case live receipt audit still validates 345 receipt references, and the original timeline asset hash remains unchanged. Live endpoint checks confirmed configured-alias consistency across all 221 events and six result surfaces in each of the three primary experiment runs. All eight combined dashboard browser checks and the production build passed. Repeated viewer tests also verified a fix for a linked-case loading race. Browser checks cover immediate node previews, search, keyboard/touch, focused case loading, model metadata, receipt jumps, and desktop/mobile popup bounds. Source archive files and native audit receipts remain unchanged; name detection remains limited to the configured alias rules.
